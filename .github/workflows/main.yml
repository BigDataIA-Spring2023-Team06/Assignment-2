name: Deploy to GCP VM

on:
  push:
    branches:
      - main

env:
  GCP_PROJECT_ID: DAMG7250 	
  GCP_VM_INSTANCE_NAME: damg7250-2

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Authenticate with GCP
        uses: google-github-actions/setup-gcloud@v2.2.0
        with:
          project_id: DAMG7250
          service_account_key: 8698f1136f664cb314b875a633d3df5dfeb31483

      - name: SSH into GCP VM
        uses: appleboy/ssh-action@master
        with:
          host: damg7250-2
          username: snehil_aryan7823
          password: Breakingbad@1
          port: 22

      - name: Run Docker container
        run: |
          sudo docker ps
